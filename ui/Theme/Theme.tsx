import { extendTheme } from "@chakra-ui/react";

const breakpoints = {
  sm: "360px",
  md: "768px",
  lg: "1280px",
  xl: "1440px",
  "2xl": "1920px",
};

export default extendTheme({ breakpoints });
